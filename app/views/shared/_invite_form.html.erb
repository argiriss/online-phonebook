<%= form_with model: @user, url: user_invitation_path do |f| %>
  <%= email_field(:user, :email, class: "form-control", placeholder: "Email") %>
  <%= submit_tag("Invite", class: "btn btn-primary") %>
<% end %>